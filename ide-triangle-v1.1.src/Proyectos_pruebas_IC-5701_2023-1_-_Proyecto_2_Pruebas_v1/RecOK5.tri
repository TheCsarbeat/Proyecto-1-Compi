! Funciones mutuamente recursivas


let

  rec

    func f (a : Integer): Integer ~ if a > 0 then g(a - 1) else 0 ! if expression no lleva end

    |

    func g (b : Integer): Integer ~ if b > 0 then f(b - 1) else 0 ! if expression no lleva end

  end

in

  putint(f(10))

end
