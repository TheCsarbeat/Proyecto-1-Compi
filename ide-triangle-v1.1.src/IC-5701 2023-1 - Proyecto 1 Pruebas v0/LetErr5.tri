let
	var i : Char;
	var j : Integer; 
	const k ~ 8;
	type dia ~ Integer ; ! el ";" _separa_ declaraciones, no las termina
      ! hay ";"  pero no hay declaración
in ! in no puede iniciar una declaración
	put(i); 
	put(i) 
end


