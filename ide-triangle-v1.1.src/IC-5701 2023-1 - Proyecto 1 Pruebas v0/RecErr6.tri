let
	rec
		proc doble ( var i : Integer ) ~
			i := i*2
		! falta el end del proc, no confundir con el del recursive
	end
in
	skip 
end

! Error: Solo hay una alternativa y, además, falta el end del proc