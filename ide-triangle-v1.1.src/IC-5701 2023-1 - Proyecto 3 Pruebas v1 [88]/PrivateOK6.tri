let

  private
    const a ~ 1; ! 1
    
    private
      const b ~ a + 1 ! b = 2, a visible aquí­
    in
      const c ~ b + a ! c = 3, a y b visibles aquí
    end
  in
    const d ~ a + c  ! d = 4, a y c visibles aquí; b no visible aquí
  end

in

  putint(d) ! imprime 4, d visible aquí­

end

! La salida debe lucir así:
! 4
