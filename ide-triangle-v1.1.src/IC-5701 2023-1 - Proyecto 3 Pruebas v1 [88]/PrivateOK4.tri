let
	private
		const a ~ 5; ! 5
		var b := 15  ! 15
	in
		var c := a * b * 2 ! 150, a y b visibles aquí
	end;
	var d := 5 ! 5
in
	putint(c + d) ! 155, c y d visibles aquí­
end

! OK

! La salida debe lucir así:
! 155
