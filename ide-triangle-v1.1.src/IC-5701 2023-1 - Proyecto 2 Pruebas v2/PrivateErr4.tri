
let

  private
    const a ~ 1;
    
    private
      const b ~ a + 1 ! a visible aquí­
    in
      const c ~ b + a ! a y b visibles aquí
    end
  in
    const d ~ a + c ! a y c visibles aquí
  end

in

  putint(a) ! a no debe ser visible aquí­

end