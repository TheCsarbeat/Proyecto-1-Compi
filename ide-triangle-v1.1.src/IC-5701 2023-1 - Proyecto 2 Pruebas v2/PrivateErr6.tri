
let

  private
    const a ~ 1
    ;
    private
      const b ~ a + 1 ! a visible aquí­
    in
      const c ~ b + a ! a y b visibles aquí
    end
  in
    const d ~ a + c ! a y c visibles aquí; b no visible aquí
  end

in

  putint(c) ! c no debe ser visible aquí­

end