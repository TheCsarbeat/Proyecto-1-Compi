! Funciones mutuamente recursivas; posterior ve a anterior


let

  rec

    func f (a : Integer): Integer ~ 1

    |

    func g (b : Integer): Integer ~ if b > 0 then f(b - 1) else 0 ! if expression no lleva end

  end

in

  putint(f(10)) 

end
