package myP ~ 
	var a: Integer
end

let
	var b: Integer
in
	b := myP . a $ b; ! hace un acceso cualificado inapropiado
	putint(b)

end

!Error: el $ solamente puede aparecer inmediatamente después del identificador de paquete, antes de seguir hacia Var-name